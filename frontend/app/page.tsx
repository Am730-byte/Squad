export default function Home() {
  return (
    <main className="page-shell">
      <div className="page-content">
        <span className="eyebrow">Frontend</span>
        <h1>Chat app frontend initialized.</h1>
        <p>
          This Next.js app is ready for the UI build. We can start shaping the
          messaging layout next.
        </p>
      </div>
    </main>
  );
}

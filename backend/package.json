{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "Prisma backend for the chat app",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^7.7.0",
    "prisma": "^7.7.0"
  }
}
